<style>
  .managed-block { padding: var(--section-space, 72px) 24px; }
  .managed-block:nth-child(even):not(.managed-hero):not(.managed-cta):not(:first-of-type) { background: #fff; }
  .managed-inner { width: min(1120px, 100%); margin: 0 auto; }
  .managed-eyebrow { color: var(--site-theme, #1457d9); font-weight: 800; text-transform: uppercase; font-size: 13px; }
  .managed-title { max-width: 820px; margin: 8px 0 14px; font-size: clamp(34px, 6vw, 62px); line-height: 1.04; font-weight: 900; }
  .managed-subheading { max-width: 780px; color: var(--muted, #5e6b80); font-size: 19px; line-height: 1.6; }
  .managed-copy { max-width: 850px; color: var(--muted, #5e6b80); white-space: pre-line; font-size: 17px; line-height: 1.75; }
  .managed-button { display: inline-flex; margin-top: 18px; padding: 11px 18px; border-radius: var(--button-radius, 6px); background: var(--site-theme, #1457d9); color: #fff; text-decoration: none; font-weight: 800; }
  .managed-button:hover { color: #fff; filter: brightness(.88); }
  .managed-hero { min-height: 64vh; display: flex; align-items: center; color: #fff; background-color: #10203d; background-image: linear-gradient(115deg, #08142c 0%, var(--site-theme, #1457d9) 100%); }
  .managed-hero .managed-eyebrow, .managed-hero .managed-subheading, .managed-hero .managed-copy { color: rgba(255,255,255,.84); }
  .managed-hero .managed-button { background: #fff; color: #10203d; }
  .managed-hero__layout { display: grid; grid-template-columns: minmax(0, 1.1fr) minmax(280px, .9fr); gap: 52px; align-items: center; }
  .managed-hero__image { display: block; width: 100%; max-height: 480px; aspect-ratio: 4 / 3; object-fit: cover; border-radius: 8px; box-shadow: 0 24px 58px rgba(0,0,0,.24); }
  .managed-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 16px; margin-top: 28px; }
  .managed-item { min-height: 112px; padding: 22px; border: 1px solid #dce4ef; border-radius: 8px; background: #fff; box-shadow: 0 8px 24px rgba(25,40,70,.06); }
  .managed-item i { color: var(--site-theme, #1457d9); }
  .managed-cta { color: #fff; background-color: #10203d; background-image: linear-gradient(115deg, #10203d 0%, var(--site-theme, #1457d9) 100%); }
  .managed-cta .managed-subheading { color: rgba(255,255,255,.82); }
  .managed-cta .managed-copy { color: rgba(255,255,255,.82); }
  .managed-block:first-of-type:not(.managed-hero):not(.managed-cta) { color: #fff; background-color: #10203d; background-image: linear-gradient(115deg, #08142c 0%, var(--site-theme, #1457d9) 100%); }
  .managed-block:first-of-type:not(.managed-hero):not(.managed-cta) .managed-eyebrow,
  .managed-block:first-of-type:not(.managed-hero):not(.managed-cta) .managed-subheading,
  .managed-block:first-of-type:not(.managed-hero):not(.managed-cta) .managed-copy { color: rgba(255,255,255,.86); }
  .managed-contact { display: grid; grid-template-columns: minmax(0, 1fr) minmax(250px, .55fr); gap: 36px; align-items: center; }
  .public-site-header { position: relative; z-index: 20; background: #fff; border-bottom: 1px solid #dce4ef; }
  .public-site-header__inner { width: min(1180px, calc(100% - 32px)); min-height: 70px; margin: 0 auto; display: flex; align-items: center; gap: 22px; }
  .public-site-brand { margin-right: auto; color: var(--site-theme, #1457d9); text-decoration: none; font-weight: 900; font-size: 19px; }
  .public-site-brand img { display: block; max-width: 220px; max-height: 54px; object-fit: contain; }
  .public-site-header.is-sticky { position: sticky; top: 0; }
  .public-site-header.style-compact .public-site-header__inner { min-height: 54px; }
  .public-site-header.style-transparent { position: absolute; width: 100%; background: rgba(255,255,255,.9); backdrop-filter: blur(12px); }
  .public-site-header.style-transparent.is-sticky { position: sticky; }
  .public-site-header.logo-size-small .public-site-brand img { max-width: 150px; max-height: 40px; }
  .public-site-header.logo-size-large .public-site-brand img { max-width: 280px; max-height: 66px; }
  .public-site-header.logo-center .public-site-header__inner { display: grid; grid-template-columns: 1fr auto 1fr; }
  .public-site-header.logo-center .public-site-brand { grid-column: 2; margin: 0; justify-self: center; }
  .public-site-header.logo-center .public-site-nav { grid-column: 1 / -1; grid-row: 2; justify-content: center; }
  .public-site-header.logo-center .public-site-actions { grid-column: 3; grid-row: 1; justify-self: end; }
  .public-site-nav { display: flex; align-items: stretch; gap: 4px; }
  .public-site-nav__item { position: relative; }
  .public-site-nav a { display: block; position: relative; padding: 13px 11px; color: #273348; text-decoration: none; font-weight: 700; font-size: 14px; }
  .public-site-nav a:hover { color: var(--site-theme, #1457d9); }
  .public-site-nav a.is-active { color: var(--site-theme, #1457d9); }
  .public-site-nav a.is-active::after { content: ''; position: absolute; left: 11px; right: 11px; bottom: 6px; height: 2px; border-radius: 1px; background: var(--site-theme, #1457d9); }
  .public-site-nav__children { display: none; position: absolute; top: 100%; left: 0; min-width: 210px; padding: 6px; border: 1px solid #dce4ef; border-radius: 6px; background: #fff; box-shadow: 0 14px 32px rgba(20,35,65,.14); }
  .public-site-nav__children a { padding: 9px 11px; }
  .public-site-nav__children a.is-active { background: rgba(20,35,65,.06); border-radius: 4px; }
  .public-site-nav__children a.is-active::after { display: none; }
  /* Third tier. The item goes position:static so the panel's containing block becomes
     .public-site-header (position:relative), letting it span the full header width. */
  .public-site-nav__item--mega { position: static; }
  .public-site-nav__mega { display: none; position: absolute; top: 100%; left: 0; right: 0; padding: 24px 0 28px; border-top: 1px solid #dce4ef; background: #fff; box-shadow: 0 18px 40px rgba(20,35,65,.14); }
  .public-site-nav__mega-inner { width: min(1180px, calc(100% - 32px)); margin: 0 auto; display: grid; grid-template-columns: repeat(auto-fit, minmax(190px, 1fr)); gap: 6px 28px; align-items: start; }
  .public-site-nav__mega-title { display: block; padding: 6px 0 8px; border-bottom: 1px solid #e6ecf5; color: #172033; font-size: 12px; font-weight: 900; letter-spacing: .06em; text-transform: uppercase; }
  .public-site-nav__mega-title.is-active { color: var(--site-theme, #1457d9); }
  .public-site-nav__mega-title.is-active::after { display: none; }
  .public-site-nav__mega-links { padding-top: 4px; }
  .public-site-nav__mega-links a { padding: 7px 0; color: #45536b; font-size: 13.5px; font-weight: 600; }
  .public-site-nav__mega-links a.is-active { color: var(--site-theme, #1457d9); font-weight: 800; }
  .public-site-nav__mega-links a.is-active::after { display: none; }
  /* Mobile-only controls; the media query below is the only place these become visible. */
  .public-site-navbtn, .public-site-nav__toggle { display: none; }
  .public-site-actions { display: flex; align-items: center; gap: 10px; white-space: nowrap; }
  .public-site-action { display: inline-flex; align-items: center; gap: 6px; color: #344158; text-decoration: none; font-size: 13px; font-weight: 700; }
  .public-site-action:hover { color: var(--site-theme, #1457d9); }
  .public-site-cta { display: inline-flex; align-items: center; min-height: 40px; padding: 0 15px; border-radius: var(--button-radius, 6px); background: var(--site-theme, #1457d9); color: #fff; text-decoration: none; font-weight: 800; font-size: 14px; }
  .public-site-cta:hover { color: #fff; filter: brightness(.9); }
  .header-dark .public-site-header { background: #111827; border-color: rgba(255,255,255,.12); }
  .header-dark .public-site-nav a, .header-dark .public-site-brand { color: #fff; }
  .header-dark .public-site-nav a.is-active { color: #fff; }
  .header-dark .public-site-nav a.is-active::after { background: #fff; }
  .header-dark .public-site-nav__children a { color: #273348; }
  .header-dark .public-site-nav__children a:hover { color: var(--site-theme, #1457d9); }
  /* The mega panel has its own white background, so its links must not inherit the dark header's
     white text -- same reason the two rules above exist for the simple dropdown. */
  .header-dark .public-site-nav__mega-title { color: #172033; }
  .header-dark .public-site-nav__mega-links a { color: #45536b; }
  .header-dark .public-site-nav__mega a:hover { color: var(--site-theme, #1457d9); }
  .header-dark .public-site-action { color: rgba(255,255,255,.86); }
  .header-overlay .public-site-header { position: absolute; z-index: 30; width: 100%; background: rgba(255,255,255,.92); }
  .template-preview-banner { position: sticky; top: 0; z-index: 100; padding: 10px 18px; background: #fff3cd; border-bottom: 1px solid #ffda6a; color: #664d03; text-align: center; font: 700 14px/1.4 Arial, sans-serif; }
  .template-preview-banner.unsaved-preview-banner { background: #cfe2ff; border-bottom-color: #9ec5fe; color: #084298; }
  .template-preview-banner.prospect-preview-banner { background: #d1fae5; border-bottom-color: #6ee7b7; color: #065f46; }
  .prospect-preview-banner a { color: #065f46; text-decoration: underline; font-weight: 800; margin-left: 6px; }
  .hero-centered .managed-hero__layout { grid-template-columns: 1fr; text-align: center; }
  .hero-centered .managed-hero__layout > div { margin: 0 auto; }
  .hero-centered .managed-hero__image { margin: 0 auto; max-width: 760px; }
  .hero-image-left .managed-hero__image { order: -1; }
  .hero-style-clean .managed-hero { color: #172033; background: #fff; border-bottom: 1px solid #dce4ef; }
  .hero-style-clean .managed-hero .managed-eyebrow { color: var(--site-theme, #1457d9); }
  .hero-style-clean .managed-hero .managed-subheading,
  .hero-style-clean .managed-hero .managed-copy { color: var(--muted, #5e6b80); }
  .hero-style-classic .managed-hero { background-image: linear-gradient(115deg, #25211d 0%, var(--site-theme, #1457d9) 100%); }
  .dyk-teasers { list-style: none; padding: 0; margin: 0 0 24px; }
  .dyk-card { padding: 16px 0; border-bottom: 1px solid rgba(0,0,0,.08); }
  .dyk-card-title { display: flex; align-items: center; margin: 0 0 8px; font-size: 17px; font-weight: 800; }
  .dyk-card-excerpt { margin: 0 0 10px; color: var(--muted, #5e6b80); font-size: 14px; line-height: 1.6; }
  .dyk-readmore { display: inline-flex; align-items: center; gap: 6px; padding: 0; border: 0; background: none; font: 800 13px Arial, sans-serif; color: var(--site-theme, #1457d9); text-decoration: none; cursor: pointer; }
  .dyk-readmore:hover { text-decoration: underline; }
  .dyk-teasers.dyk-grid-2x3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; }
  .dyk-teasers.dyk-grid-2x3 .dyk-card { border-bottom: 0; padding: 16px; background: rgba(0,0,0,.03); border-radius: 8px; }
  /* Hover reveal is desktop-only: below 801px the accordion in _PublicNavigation.cshtml drives
     visibility off .is-open instead, so these rules must not apply there. */
  @@media (min-width: 801px) {
    .public-site-nav__item:hover .public-site-nav__children { display: block; }
    .public-site-nav__item--mega:hover .public-site-nav__mega { display: block; }
  }
  @@media (min-width: 901px) {
    body.header-sidebar { padding-left: 240px; }
    .header-sidebar .public-site-header { position: fixed; inset: 0 auto 0 0; width: 240px; overflow-y: auto; border-right: 1px solid #dce4ef; border-bottom: 0; }
    .header-sidebar .public-site-header__inner { width: 100%; min-height: 100%; padding: 28px 18px; flex-direction: column; align-items: stretch; }
    .header-sidebar .public-site-brand { margin: 0 0 24px; }
    .header-sidebar .public-site-nav { flex-direction: column; }
    .header-sidebar .public-site-nav__children { position: static; box-shadow: none; }
    /* A full-width hover panel makes no sense inside a 240px rail -- render the third tier as a
       always-expanded indented tree instead. */
    .header-sidebar .public-site-nav__item--mega { position: relative; }
    .header-sidebar .public-site-nav__mega { display: block; position: static; padding: 0 0 6px 10px; border-top: 0; box-shadow: none; }
    .header-sidebar .public-site-nav__mega-inner { width: 100%; display: block; }
    .header-sidebar .public-site-nav__mega-title { padding: 8px 0 6px; }
    .header-sidebar .public-site-nav__mega-links { padding-left: 10px; }
  }
  @@media (max-width: 800px) {
    .public-site-header__inner { position: relative; align-items: flex-start; flex-direction: column; padding: 14px 0; }
    .public-site-header.logo-center .public-site-header__inner { display: flex; }
    .public-site-actions { width: 100%; flex-wrap: wrap; }
    .public-site-action span { display: none; }
    /* Absolutely positioned so the hamburger sits beside the brand without restructuring the
       header markup, which stays a plain column stack at this width. */
    .public-site-navbtn { display: flex; flex-direction: column; justify-content: center; gap: 5px; position: absolute; top: 12px; right: 0; width: 42px; height: 38px; padding: 0 9px; border: 1px solid #dce4ef; border-radius: 6px; background: #fff; cursor: pointer; }
    .public-site-navbtn span { display: block; height: 2px; border-radius: 1px; background: #273348; }
    .header-dark .public-site-navbtn { border-color: rgba(255,255,255,.28); background: transparent; }
    .header-dark .public-site-navbtn span { background: #fff; }
    .public-site-nav { display: none; flex-direction: column; align-items: stretch; width: 100%; gap: 0; }
    .public-site-nav.is-open { display: flex; }
    .public-site-nav__item { display: flex; flex-wrap: wrap; align-items: center; border-top: 1px solid #eef2f8; }
    .public-site-nav__item > a { flex: 1 1 auto; }
    .public-site-nav__toggle { display: block; position: relative; width: 44px; height: 42px; border: 0; background: transparent; cursor: pointer; }
    .public-site-nav__toggle::after { content: ''; position: absolute; top: 50%; left: 50%; width: 8px; height: 8px; margin: -6px 0 0 -4px; border-right: 2px solid #5e6b80; border-bottom: 2px solid #5e6b80; transform: rotate(45deg); }
    .public-site-nav__item.is-open > .public-site-nav__toggle::after { margin-top: -2px; transform: rotate(225deg); }
    .header-dark .public-site-nav__toggle::after { border-color: rgba(255,255,255,.72); }
    .public-site-nav__children, .public-site-nav__mega { display: none; position: static; flex-basis: 100%; padding: 0 0 8px 14px; border: 0; border-radius: 0; background: transparent; box-shadow: none; }
    .public-site-nav__item.is-open > .public-site-nav__children, .public-site-nav__item.is-open > .public-site-nav__mega { display: block; }
    .public-site-nav__mega-inner { display: block; width: 100%; }
    .public-site-nav__mega-col { padding: 4px 0; }
    .public-site-nav__mega-links { padding-left: 12px; }
    .header-dark .public-site-nav__children a, .header-dark .public-site-nav__mega-links a { color: rgba(255,255,255,.86); }
    .header-dark .public-site-nav__mega-title { color: #fff; border-bottom-color: rgba(255,255,255,.18); }
    .managed-block { padding: min(var(--section-space, 48px), 48px) 20px; }
    .managed-grid { grid-template-columns: 1fr; }
    .managed-contact { grid-template-columns: 1fr; }
    .managed-hero__layout { grid-template-columns: 1fr; }
    .dyk-teasers.dyk-grid-2x3 { grid-template-columns: 1fr; }
  }
</style>
